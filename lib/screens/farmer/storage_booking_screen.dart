// lib/screens/farmer/storage_booking_screen.dart
import 'package:flutter/material.dart';
// import '../../utils/constants.dart';

class StorageBookingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Book Storage'),
      ),
      body: Center(
        child: Text('Storage Booking Screen - Coming Soon'),
      ),
    );
  }
}


// Placeholder Profile Screen
class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Profile'),
      ),
      body: Center(
        child: Text('Profile Screen - Coming Soon'),
      ),
    );
  }
}