import 'package:flutter/material.dart';

class AvailableJobsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Available Jobs'),
      ),
      body: Center(
        child: Text('Available Jobs Screen - Coming Soon'),
      ),
    );
  }
}